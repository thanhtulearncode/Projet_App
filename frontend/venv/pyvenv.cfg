home = /Library/Developer/CommandLineTools/usr/bin
include-system-site-packages = false
version = 3.9.6

import React, { useState } from 'react';
import Game from './components/Game';
import Menu from './components/Menu';
import './index.css';

function App() {
  const [gameStarted, setGameStarted] = useState(false);
  const [gameSettings, setGameSettings] = useState(null);

  const handleStartGame = (settings) => {
    setGameSettings(settings);
    setGameStarted(true);
  };

  return (
    <div className="app">
      {!gameStarted ? (
        <Menu onStartGame={handleStartGame} />
      ) : (
        <Game settings={gameSettings} />
      )}
    </div>
  );
}

export default App;

/* Supprimer ou commenter cette ligne */
/* background-image: url('./images/wallstreet_background.jpg'); */

/* Si cette ligne est dans un bloc CSS comme .menu-container, 
   remplacez-la par une couleur de fond simple */
.menu-container {
  /* Supprimer cette ligne */
  /* background-image: url('./images/wallstreet_background.jpg'); */
  
  /* Remplacer par une couleur de fond simple */
  background-color: #2c3e50; /* Couleur bleu foncé */
}
